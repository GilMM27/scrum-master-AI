info:
  name: Get Project Analytics
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/analytics/project/{{ProjectVar}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
