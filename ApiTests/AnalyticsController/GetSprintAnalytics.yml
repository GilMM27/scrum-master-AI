info:
  name: Get Sprint Analytics
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/analytics/sprint/{{sprint}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
