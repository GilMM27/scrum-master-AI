info:
  name: LogInValido
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/auth/login"
  body:
    type: json
    data: |-
      {
        "username": "{{username}}",
        "password":"{{password}}"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setVar("BearThoken", res.getBody().token)

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
