info:
  name: GetAllUsers
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/users"
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setVar("userId",res.getBody()[0].userId)

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
