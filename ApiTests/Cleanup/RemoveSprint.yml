info:
  name: Remove Sprint
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseUrl}}/api/sprints/{{sprint}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
