info:
  name: Remove Project
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/api/projects/{{ProjectVar}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
