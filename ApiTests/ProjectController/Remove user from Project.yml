info:
  name: Remove user from Project
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/projects/{{ProjectVar}}/members/{{userId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
