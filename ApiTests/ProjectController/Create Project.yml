info:
  name: Create Project
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/api/projects"
  body:
    type: json
    data: |-
      {
        "name": "My Project For testing",
        "description": "Optional description"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setVar("ProjectVar", res.getBody().projectId)

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
