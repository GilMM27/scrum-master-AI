info:
  name: AddUserToProject copy
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/projects/{{ProjectVar}}/members/{{userId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
