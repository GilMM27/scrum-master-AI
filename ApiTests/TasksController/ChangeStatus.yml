info:
  name: ChangeStatus
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/api/tasks/{{TestTaskId}}/status"
  body:
    type: json
    data: |-
      {
        "status":"IN_PROGRESS"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
