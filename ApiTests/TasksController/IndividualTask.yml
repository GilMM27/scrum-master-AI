info:
  name: IndividualTask
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/tasks/{{TestTaskId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
