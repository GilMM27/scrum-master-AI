info:
  name: GetBlocked
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/api/tasks/blocked"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
