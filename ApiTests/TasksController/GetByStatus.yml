info:
  name: GetByStatus
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/tasks/status/IN_PROGRESS"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
