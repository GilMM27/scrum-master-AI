info:
  name: Assing Task to user
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/tasks/{{TestTaskId}}/assignees/{{userId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
